# Test-only dependencies — not installed in the deployed image.
#   pip install -r requirements.txt -r requirements-dev.txt
#   python3 -m pytest -q
pytest==9.1.1
# Starlette's TestClient, which every API test goes through, needs httpx.
httpx==0.28.1
